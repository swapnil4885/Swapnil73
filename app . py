from flask import Flask, request

app = Flask(__name__)

@app.route('/')
def home():
    return "✅ Shiv Pro Trading Bot is Live on Render!"

@app.route('/webhook', methods=['POST'])
def webhook():
    data = request.json
    return {"status": "success", "data": data}, 200

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000)
